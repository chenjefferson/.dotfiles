[core]
	autocrlf = false
    safecrlf = true
    editor = vim
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    bh = log --oneline --abbrev-commit --all --graph --decorate --color
    type = cat-file -t
    dump = cat-file -p
    files = ls-tree -r master --name-only
[user]
    name = Jefferson William Chen
    email = jeffwilliamchen@ieee.org
[push]
    default = matching
